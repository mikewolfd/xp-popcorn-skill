# Popcorn XP — Codex hooks + multi-agent (see docs/dual-mode-codex-companion.md)
# Merge with your existing ~/.codex/config.toml as needed.

[features]
codex_hooks = true

[agents]
max_threads = 8
max_depth = 1
